# TODO: Fix Maven Compilation Errors

- [ ] Add getPrimaryRole() method to User.java to return the name of the first role in the set
- [ ] Add findNearby() method to KioskService.java that delegates to KioskRepository.findNearby()
- [ ] Change kiosk.setStreet(street) to kiosk.setTown(street) in KioskService.java
- [ ] Run mvn clean install to verify fixes
